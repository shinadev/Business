services:
  - type: web
    name: business-app
    env: dotnet
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/Business.dll
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
