@model Business.Models.Blog.PlainText

@{
    ViewData["Title"] = "Edit PlainText";
    Layout = "~/Views/Shared/Admin.cshtml";
}

<div class="container py-4">
    <div class="card shadow-lg border-0 rounded-lg">
        <div class="card-header bg-warning text-dark d-flex justify-content-between align-items-center">
            <h4 class="mb-0"><i class="fas fa-edit me-2"></i> Edit PlainText</h4>
            <a asp-action="Index" class="btn btn-light btn-sm">
                <i class="fas fa-arrow-left"></i> Back to List
            </a>
        </div>
        <div class="card-body p-4">
            <form asp-action="Edit">
                <input type="hidden" asp-for="Id" />
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                <div class="mb-3">
                    <label asp-for="Content" class="form-label fw-bold"></label>
                    <textarea asp-for="Content" class="form-control shadow-sm" rows="5" placeholder="Edit content..."></textarea>
                    <span asp-validation-for="Content" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="ReadMoreLink" class="form-label fw-bold"></label>
                    <input asp-for="ReadMoreLink" class="form-control shadow-sm" placeholder="e.g. /blog/read-more" />
                    <span asp-validation-for="ReadMoreLink" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="BlogDetailId" class="form-label fw-bold">Related Blog</label>
                    <select asp-for="BlogDetailId" class="form-select shadow-sm" asp-items="ViewBag.BlogDetailId">
                        <option value="">-- Select a Blog --</option>
                    </select>
                    <span asp-validation-for="BlogDetailId" class="text-danger small"></span>
                </div>

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-success px-4">
                        <i class="fas fa-save me-1"></i> Save
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
