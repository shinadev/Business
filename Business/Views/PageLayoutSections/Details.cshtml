@model Business.Models.Page.PageLayoutSection

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/Admin.cshtml";
}

<h1 class="mb-4">Page Layout Section Details</h1>

<div>
    <h4>PageLayoutSection</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Page)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.Page.Slug)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.LayoutSection)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.LayoutSection.Name)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Title)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.Title)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Content)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.Content)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.SortOrder)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.SortOrder)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.IsActive)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.IsActive)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.CreatedAt)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.CreatedAt)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.UpdatedAt)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.UpdatedAt)</dd>
    </dl>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary me-2">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>
